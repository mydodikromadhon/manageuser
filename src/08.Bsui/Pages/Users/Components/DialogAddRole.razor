<MudDialog>
    <DialogContent>
        <LoadingLinear IsVisible="_isLoading" />
        <EditForm Model="Request" OnInvalidSubmit="OnInvalidSubmit" OnValidSubmit="OnValidSubmit">
            <MudFocusTrap>
                <MudGrid>
                    <MudItem xs="12">
                        <MudSelect
                            Label="Select Roles"
                            Required="true"
                            @bind-Value="SelectedRole">

                            @foreach (var identityRole in IdentityRoles)
                            {
                                <MudSelectItem Value="identityRole.Name">@identityRole.Name</MudSelectItem>
                            }

                        </MudSelect>
                    </MudItem>
                </MudGrid>
            </MudFocusTrap>
            <BottomButtonPanel>
                <MudButton Variant="Variant.Filled" Color="Color.Default" OnClick="Cancel">Cancel</MudButton>
                <MudButton Variant="Variant.Filled" Color="Color.Primary" ButtonType="ButtonType.Submit">Add</MudButton>
            </BottomButtonPanel>
        </EditForm>
    </DialogContent>
</MudDialog>
